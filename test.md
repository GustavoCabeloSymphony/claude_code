# Title

# Subtitle

Here's some code without a language tag:

```python
def hello():
    print("world")
```

And here's JSON without a tag:

```json
{
  "name": "test",
  "value": 123
}
```

Excessive blank lines above should be cleaned up.

New code without a tag:

```javascript
function test() {
  console.log("test");
}
```
```python
def hello_world():
    print("hello world")
```

## Testing Hook Trigger

Here's some TypeScript without a tag:

```text
interface User {
  name: string;
  age: number;
}
```

And some SQL:

```sql
SELECT * FROM users WHERE age > 18;
```
